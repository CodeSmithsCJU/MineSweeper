#mine = -1
#flag = -2
#답지는 폭탄 -1과 숫자로 써져있는 완성된 배열
#풀이는 연산 시행 부분 대조한 뒤 가져오기
def leftclick():
    if(b_open[x][y] == True):
        #낫띵해쁜드
        return
    if(b_close[x][y] == True):
        if():
            return
        if():
            return
        if():
            return
        if():
            return
        if():
            return
        #재귀저기연산 시작
        return
    if(b[x][y] == -1):
        #calling gameover 
        return
    
def rightclick():
    if(b[x][y]==nonclick):
        #깃발 이미지로 변경
        return
    if(b[x][y] == -2):
        #칸 기본 이미지로 변경
        return
    if(b[x][y] == -1):
        if(-1 == -2):
            #게임 승리 불러오기
            return